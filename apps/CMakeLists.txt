add_executable(app app.cpp)
add_executable(app2 app2.cpp)

target_link_libraries(app PRIVATE old_quadtree)
target_link_libraries(app2 PRIVATE libquadtree)

if (TIME_READ_FILE)
    target_compile_definitions(app2 PRIVATE TIME_READ_FILE)
endif ()
if (TIME_FLATTEN)
    target_compile_definitions(app2 PRIVATE TIME_FLATTEN)
endif ()
if (TIME_QUADTREE_BUILD)
    target_compile_definitions(app2 PRIVATE TIME_QUADTREE_BUILD)
endif ()
if (TIME_COLORIZE)
    target_compile_definitions(app2 PRIVATE TIME_COLORIZE)
endif ()
if (TIME_WRITE_FILE)
    target_compile_definitions(app2 PRIVATE TIME_WRITE_FILE)
endif ()